paper_parser_paper_sections = """
    "首页":["论文题目","副题目","作者姓名","作者单位","摘要","关键词","中图分类号","英文题目","英文作者姓名","英文作者单位","英文摘要","英文关键词"],
    "正文":["正文段落","一级标题","二级标题","三级标题","图表","公式","算法","代码片段"],
    "参考文献":["文献列表"],
    "作者简介":[],
    "作者贡献声明":[],
    "课题或基金项目":[]
"""
input_parser_paper_sections = [
        "页面布局",
        "论文题目",
        "副题目",
        "作者姓名",
        "作者单位",
        "摘要",
        "关键词",
        "中图分类号",
        "英文题目",
        "英文作者姓名",
        "英文作者单位",
        "英文摘要",
        "英文关键词",
        "正文段落",
        "一级标题",
        "二级标题",
        "三级标题",
        "图表",
        "公式",
        "算法",
        "代码片段",
        "文献列表",
        "作者简介",
        "作者贡献声明",
        "课题或基金项目",
    ]
layout_agent_rules = {
    "cover": [
        "论文题目",
        "副题目",
        "作者姓名",
        "作者单位",
        "摘要",
        "关键词",
        "中图分类号",
        "英文题目",
        "英文作者姓名",
        "英文作者单位",
        "英文摘要",
        "英文关键词",
    ],
    "text": [
        "正文段落",
        "一级标题",
        "二级标题",
        "三级标题",
        "正文",
        "公式",
        "算法",
        "代码片段",
    ],
    "reference": ["文献列表", "参课文献"],
    "fund": ["课题或基金项目", "基金", "基金项目", "横向项目"],
    "chart": ["图片", "表格", "图表"],
    "author": ["作者简介", "作者贡献声明"],
    "other": [],
}

input_check_prompt = """
    你是一名学术论文排版大师，专门负责对论文中的格式进行调整。
    现在需要你对用户的输入进行分析并判断是否属于对论文排版的请求或需求。
    如果有关，那就返回：True；
    如果无关，那就返回：False；
    除了这两个选项外，不要返回任何其他内容。辛苦你啦！~
"""
llm_output_prompt = """
    你是一个论文排版助手的前端响应生成器。用户在提交任务时出现了问题，你需要根据问题类型，生成一段友好、清晰、有引导性的中文提示。
    要求：
    - 语气专业且亲切，不要冷冰冰
    - 明确指出问题
    - 给出下一步操作建议
    - 不要使用 markdown，纯文本
    - 控制在 3 句话以内
    
    可能存在的问题类型如下：
    - 用户未输入需求！ → 提示用户输入需求
    - 用户需求超出agent任务范围！ → 委婉说明无法处理，并举例说明能做什么
    - 用户未上传论文文件！ → 提示上传论文文件
    
    请生成合适的提示语：
"""
input_parser_prompt = """
    用户输入的排版要求：{input}
    请将排版要求按以下论文结构模块分类提取，每个模块用列表形式列出具体规则（如字体、字号、对齐等），未知项可省略或填"无"：
    支持模块：{fields_desc}
    输出格式如下（仅输出 JSON 字典，不要解释）：
    {{
      "论文题目": [
        {{"字体": "XXX"}},
        {{"字号": "XXX"}},
        {{"加粗": "是/否"}},
        {{"对齐": "XXX"}}
      ],
      "作者": [...],
      ...
    }}
    提取结果：
    {output}
"""
input_parser_prompt_examples = [
    {
        "input": "题目黑体三号居中，英文题目也一样但用Times New Roman。摘要宋体小四，英文摘要同中文但字体为Times New Roman。",
        "output": {
            "论文题目": [{"字体": "黑体"}, {"字号": "三号"}, {"对齐": "居中"}],
            "英文题目": [{"字体": "Times New Roman"}, {"字号": "三号"}, {"对齐": "居中"}],
            "摘要": [{"字体": "宋体"}, {"字号": "小四"}],
            "英文摘要": [{"字体": "Times New Roman"}, {"字号": "小四"}]
        }
    }
]

input_parser_filter_prompt ="""
你是一个文本过滤器，专门处理论文排版相关的自然语言描述。

请执行以下任务：
1. 保留所有论文排版、文字排版或格式要求等样式描述内容。
2. 删除以下内容：
- 参考文献的具体条目（如作者、标题、期刊名）
- 内容写作要求（如“逻辑清晰”“语言通顺”）
- 非格式类指令（如“请认真完成”“不要抄袭”）
- 数据、公式、图表内容本身

输出：仅返回过滤后的文本，不要解释。
"""

paper_parser_system_prompt = """
你是一个专业的学术论文结构分析大师。论文结构中包含的类型如下：
{paper_sections}

请从论文全文中，按上述类别识别并标注出各个部分的内容，并按照指定 JSON 格式输出。

### 核心要求：
1. **严格保留原文内容**：输出的 "content" 字段必须是原文的完整字符串，包括“摘要：”“关键词：”“1 引言”等所有字符，不得删减、改写或提取。
2. **保持原文顺序**：输出的 result 列表必须严格按照原文段落顺序排列，不能打乱。
3. **细粒度识别**：每个段落独立识别，即使属于同一类（如多个“正文段落”），也要分别输出。
4. **标注段落来源**：每个识别项需保留原始段落索引（用 "para_index" 字段表示）。

### 输出格式：
{{{{
  "result": [
    {{{{
      "section": "首页",
      "subsection": "论文题目",
      "content": "基于深度学习的图像识别方法研究",
      "para_index": 0
    }}}},
    {{{{
      "section": "首页",
      "subsection": "关键词",
      "content": "关键词：中医古籍；知识表示；多粒度；知识元；知识表示模型",
      "para_index": 5
    }}}},
    {{{{
      "section": "正文",
      "subsection": "一级标题",
      "content": "1 引言",
      "para_index": 6
    }}}},
    {{{{
      "section": "正文",
      "subsection": "正文段落",
      "content": "近年来，深度学习在图像识别领域取得了显著进展...",
      "para_index": 7
    }}}}
  ]
}}}}

注意：即使多个段落都属于“正文段落”，也要分别输出，确保顺序和索引一致！
"""
layout_check_system_prompt = """
你是一个排版任务补漏大师。请根据【检验反馈】，分析哪些原始排版要求未被满足，并生成一个**标准排版任务列表**。

### 输入背景
- 用户原始需求已通过 parser_check_node 拆分为多个 `request` 项
- 上一轮排版 agent 执行了部分要求
- 现在你需要**只补漏未完成的部分**
- 输出格式必须遵守输出规则

### 输出要求（严格遵守）
1. 只返回一个 Python 列表（List[Dict]），每个元素是一个任务项
2. 每个任务项必须包含以下 **4 个字段**：
   - `"section"`: 章节名称（如 "首页"）
   - `"subsection"`: 子章节/模块名称（如 "论文题目"）
   - `"content"`: 当前内容（若无法提取可为空字符串）
   - `"request"`: 未完成的原始要求列表，格式为 `[{{"要求名": "具体说明"}}, ...]`
3. 如果某个 `subsection` 有多个未满足要求，请全部放入 `request` 列表中
4. 如果没有需要补漏的，返回空列表 `[]`
5. 不要任何额外解释、不要 Markdown、不要 JSON 包装，只返回原始列表

### 示例输出格式（必须严格对齐）
[
    {{
        "section": "首页",
        "subsection": "论文题目",
        "content": "基于《金匮要略》的多粒度知识表示模型构建研究",
        "request": [
            {{"字体": "宋体"}},
            {{"字体大小": "二号"}}
        ]
    }},
    {{
        "section": "引言",
        "subsection": "研究背景",
        "content": "中医药信息化发展迅速...",
        "request": [
            {{"行距": "1.5倍行距"}}
        ]
    }}
]

### 特别注意
- `"request"` 中每个要求必须是 `{{"key": "value"}}` 的字典格式
- 不要合并多个要求到一个字符串中
- 如果反馈中未明确 `content`，可留空，但字段必须存在

### 检验反馈
{feedback}
"""
validator_system_prompt = """
你是一个严格的排版任务审查专家。请根据用户的原始请求，审查 AI 是否正确执行了每一个任务。

输入格式：
[
  {{
    "agent_name": "author",
    "original_request": {{ "section": "摘要", "subsection": "字体", "request": [{{"字体": "宋体"}}] }},
    "execution_result": {{ "section": "摘要", "subsection": "字体", "result": "宋体" }}
  }},
  ...
]

请返回 JSON 格式：
{{
  "verification_result": [
    {{
      "agent_name": "author",
      "section": "摘要",
      "subsection": "字体",
      "request": {{"字体": "宋体"}},
      "status": "success|failed",
      "reason": "解释原因"
    }}
  ]
}}
"""